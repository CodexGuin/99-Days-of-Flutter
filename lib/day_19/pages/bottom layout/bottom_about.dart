import 'package:flutter/material.dart';

class BottomAbout extends StatelessWidget {
  const BottomAbout({super.key});

  @override
  Widget build(BuildContext context) {
    return Flexible(child: Container());
  }
}
