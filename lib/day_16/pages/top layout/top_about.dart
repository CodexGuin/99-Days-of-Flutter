import 'package:flutter/material.dart';

class TopAbout extends StatelessWidget {
  const TopAbout({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('TopAbout');
  }
}
