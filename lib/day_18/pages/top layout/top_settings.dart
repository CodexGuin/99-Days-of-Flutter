import 'package:flutter/material.dart';

class TopSettings extends StatelessWidget {
  const TopSettings({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('TopSettings');
  }
}
