import 'package:flutter/material.dart';

class TopContact extends StatelessWidget {
  const TopContact({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('TopContact');
  }
}
